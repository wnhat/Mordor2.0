<Window x:Class="EyeOfSauron.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EyeOfSauron" 
        xmlns:viewmodel="clr-namespace:EyeOfSauron.ViewModel" 
        d:DataContext="{d:DesignInstance viewmodel:MainWindowViewModel}"
        mc:Ignorable="d"
        ResizeMode="CanResize"
        WindowStyle="ThreeDBorderWindow"
        MaxHeight="2500"
        MaxWidth="3840"
        Title="InspWindow" 
        Height="2500" Width="3840" 
        WindowStartupLocation="CenterScreen" 
        MinHeight="500" MinWidth="800" 
        SizeToContent="Manual" 
        WindowState="Maximized">
    <Viewbox>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <UserControl DataContext="{Binding userInfo}"/>
            <ContentControl
                            DataContext="{Binding userInfo}"/>
        </Grid>
    </Viewbox>
</Window>
