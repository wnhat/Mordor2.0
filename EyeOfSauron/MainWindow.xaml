<Window
    x:Class="EyeOfSauron.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Converter="clr-namespace:EyeOfSauron.Converter"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:system="clr-namespace:System;assembly=mscorlib"
    xmlns:uc="clr-namespace:EyeOfSauron.MyUserControl"
    xmlns:viewModel="clr-namespace:EyeOfSauron.ViewModel"
    Title="Eye of sauron"
    Width="1600"
    Height="1080"
    MinWidth="800"
    MinHeight="500"
    MaxWidth="3840"
    MaxHeight="2560"
    d:DataContext="{d:DesignInstance viewModel:MainWindowViewModel}"
    AutomationProperties.Name="{Binding Title, RelativeSource={RelativeSource Self}}"
    Background="{DynamicResource MaterialDesignPaper}"
    Closing="Window_Closing"
    IsTabStop="False"
    KeyDown="Window_KeyDown"
    Style="{StaticResource MaterialDesignWindow}"
    TextElement.Foreground="{DynamicResource MaterialDesignBody}"
    WindowStartupLocation="CenterScreen"
    WindowState="Maximized"
    mc:Ignorable="d">
    <Window.CommandBindings>
        <CommandBinding Command="Copy" Executed="OnCopy" />
    </Window.CommandBindings>
    <Window.InputBindings>
        <KeyBinding Command="{Binding InspImageView.DataContext.InspImage.RefreshImageCommand}" Gesture="Tab" />
    </Window.InputBindings>
    <Window.Resources>
        <ResourceDictionary>
            <Converter:EnumToVisibilityConverter x:Key="EnumToVisibilityConverter" />
            <Converter:InverseEnumToVisibilityConverter x:Key="InverseEnumToVisibilityConverter" />
            <Converter:EnumToStretchConverter x:Key="EnumToStretchConverter" />
        </ResourceDictionary>
    </Window.Resources>

    <materialDesign:DialogHost
        DialogTheme="Inherit"
        Identifier="MainWindowDialog"
        x:Name="MainWindowDialog"
        SnackbarMessageQueue="{Binding ElementName=MainSnackbar, Path=MessageQueue}">
        <materialDesign:DrawerHost IsRightDrawerOpen="{Binding ElementName=MenuToggleButton, Path=IsChecked}">
            <materialDesign:DrawerHost.RightDrawerContent>
                <DockPanel MinWidth="220">
                    <ToggleButton
                        Margin="16"
                        HorizontalAlignment="Right"
                        DockPanel.Dock="Top"
                        IsChecked="{Binding ElementName=MenuToggleButton, Path=IsChecked, Mode=TwoWay}"
                        Style="{StaticResource MaterialDesignHamburgerToggleButton}" />
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1000" />
                        </Grid.ColumnDefinitions>
                        <ContentPresenter Content="{Binding ColorTool}" />
                    </Grid>
                </DockPanel>
            </materialDesign:DrawerHost.RightDrawerContent>
            <DockPanel>
                <materialDesign:ColorZone
                    Padding="12"
                    materialDesign:ShadowAssist.ShadowDepth="Depth2"
                    DockPanel.Dock="Top"
                    Mode="PrimaryMid">
                    <DockPanel>
                        <StackPanel Orientation="Horizontal">
                            <!--<Button
                                Margin="16,0,0,0"
                                materialDesign:RippleAssist.Feedback="{Binding RelativeSource={RelativeSource Self}, Path=Foreground, Converter={StaticResource BrushRoundConverter}}"
                                Click="LoginButton_Click"
                                Content="{materialDesign:PackIcon Kind=Account,
                                                                  Size=24}"
                                Foreground="{Binding RelativeSource={RelativeSource AncestorType={x:Type FrameworkElement}}, Path=(TextElement.Foreground)}"
                                Style="{StaticResource MaterialDesignToolButton}"
                                ToolTip="Login" />-->

                            <materialDesign:Chip
                                Click="LoginButton_Click"
                                Content="{Binding UserInfo.User.Username, FallbackValue=UserName}"
                                Icon="{Binding UserInfo.User.UserIcon, FallbackValue=U}"
                                IconBackground="{DynamicResource PrimaryHueLightBrush}"
                                IconForeground="{DynamicResource PrimaryHueLightForegroundBrush}"
                                IsTabStop="False" />
                            <materialDesign:PopupBox
                                IsTabStop="False"
                                PlacementMode="BottomAndAlignRightEdges"
                                StaysOpen="False">
                                <materialDesign:PopupBox.ToggleContent>
                                    <materialDesign:PackIcon Kind="ChevronDown" />
                                </materialDesign:PopupBox.ToggleContent>
                                <StackPanel>
                                    <Button
                                        HorizontalAlignment="Center"
                                        Click="UserChangeButton_Click"
                                        Content="{materialDesign:PackIcon Kind=AccountCircle}"
                                        ToolTip="User Change" />
                                    <Button
                                        HorizontalAlignment="Center"
                                        Click="LogoutButton_Click"
                                        Content="{materialDesign:PackIcon Kind=Logout}"
                                        ToolTip="Logout" />
                                </StackPanel>
                            </materialDesign:PopupBox>
                            <!--<materialDesign:PackIcon VerticalAlignment="Center" Kind="AlertBox" materialDesign:HintAssist.HelperText="aaaa" />-->
                            <Button
                                VerticalAlignment="Center"
                                HorizontalContentAlignment="Center"
                                Content="{Binding InspImageView.DataContext.PanelId, FallbackValue={}}"
                                IsTabStop="False"
                                MouseDoubleClick="PanelidLableMouseDoubleClick"
                                Style="{StaticResource MaterialDesignFlatMidBgButton}"
                                ToolTip="Double Click to Copy"
                                Visibility="{Binding OnShowView, Converter={StaticResource InverseEnumToVisibilityConverter}, Mode=TwoWay}" />

                            <Separator
                                Width="2"
                                IsTabStop="False"
                                Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" />
                            <TextBlock
                                VerticalAlignment="Center"
                                FontSize="14"
                                Text="{Binding DateTime, StringFormat={}{0:yyyy-MM-dd HH:mm:ss}}" />
                        </StackPanel>

                        <ToggleButton
                            x:Name="MenuToggleButton"
                            AutomationProperties.Name="HamburgerToggleButton"
                            Click="ColorToolToggleButton_OnClick"
                            DockPanel.Dock="Right"
                            IsChecked="False"
                            IsTabStop="False"
                            Style="{StaticResource MaterialDesignHamburgerToggleButton}" />
                        <materialDesign:Badged
                            x:Name="EndInspButton"
                            Margin="10,0,20,0"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Badge="{Binding InspImageView.DataContext.RemainingCount, FallbackValue=0}"
                            BadgeColorZoneMode="PrimaryLight"
                            DockPanel.Dock="Right"
                            Visibility="{Binding OnShowView, Converter={StaticResource InverseEnumToVisibilityConverter}, Mode=TwoWay, FallbackValue=Collapsed}">
                            <Button
                                Command="{Binding EndInspCommand}"
                                Content="End Insp"
                                IsTabStop="False"
                                Style="{StaticResource MaterialDesignRaisedLightButton}" />
                        </materialDesign:Badged>

                        <materialDesign:Badged
                            x:Name="StartInspButton"
                            Margin="10,0,20,0"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Badge="{Binding ProductSelectView.DataContext.SelectedProductCardViewModel.ProductInfo.Value}"
                            BadgeColorZoneMode="PrimaryLight"
                            DockPanel.Dock="Right"
                            Visibility="{Binding OnShowView, Converter={StaticResource EnumToVisibilityConverter}, Mode=TwoWay}">
                            <Button
                                Command="{Binding StartInspCommand}"
                                CommandParameter="{Binding ProductSelectView.DataContext.ControlTabSelectedIndex}"
                                Content="Start Insp"
                                IsTabStop="False"
                                Style="{StaticResource MaterialDesignRaisedLightButton}" />
                        </materialDesign:Badged>

                        <Button
                            Margin="10,0,10,0"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Content="{Binding ProductSelectView.DataContext.SelectedProductCardViewModel.ProductInfo.Key.Name, FallbackValue=MODEL}"
                            DockPanel.Dock="Right"
                            IsTabStop="False"
                            Style="{StaticResource MaterialDesignFlatLightBgButton}" />
                        <Separator
                            Width="2"
                            DockPanel.Dock="Right"
                            IsTabStop="False"
                            Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" />
                        <Button
                            Margin="0,0,10,0"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Click="SampleViewButton_Click"
                            Content="SampleView"
                            DockPanel.Dock="Right"
                            IsTabStop="False"
                            Style="{StaticResource MaterialDesignRaisedLightButton}" />
                        <TextBlock Text="" />
                    </DockPanel>
                </materialDesign:ColorZone>
                <ProgressBar IsIndeterminate="True" Visibility="Collapsed" />
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid
                        x:Name="MainContent"
                        Grid.Column="0"
                        Margin="0,5,0,0"
                        VerticalAlignment="Top"
                        materialDesign:ScrollViewerAssist.IsAutoHideEnabled="True">
                        <ContentPresenter Content="{Binding MainContent}" />
                    </Grid>
                    <Viewbox
                        x:Name="DefectJudgeViewbox"
                        Grid.Column="1"
                        Margin="0,5,0,0"
                        Visibility="{Binding OnShowView, Converter={StaticResource InverseEnumToVisibilityConverter}, Mode=TwoWay}">
                        <ContentPresenter Content="{Binding DefectJudgeView}" />
                    </Viewbox>
                    <materialDesign:Snackbar
                        x:Name="MainSnackbar"
                        VerticalAlignment="Bottom"
                        HorizontalContentAlignment="Center"
                        IsTabStop="False"
                        MessageQueue="{materialDesign:MessageQueue}" />
                </Grid>
            </DockPanel>
        </materialDesign:DrawerHost>
    </materialDesign:DialogHost>
</Window>
